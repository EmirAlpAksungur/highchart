import React from 'react'

const labels = () => {
    
    return (
        <div>
            HTML labels that can be positioned anywhere in the chart area.

            This option is deprecated since v7.1.2. Instead, use annotations that support labels.
        </div>
    )
}

export default labels